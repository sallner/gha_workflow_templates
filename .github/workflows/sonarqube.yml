name: SonarQube

on:
  workflow_call:
    inputs:
      project_type:
        default: "python"
        required: false
        type: string
    secrets:
      SONARQUBETOKEN:
        required: true

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Download Artifacts
      uses: actions/download-artifact@v3
      with:
        name: coverage-xml
    - name: SonarQube Scan
      if: ${{ !github.event.number }}
      uses: risclog-solution/sonarqube-action@master
      with:
        host: https://sonarqube.risclog.fcio.net
        login: ${{ secrets.SONARQUBETOKEN }}
        projectType: ${{ inputs.project_type }}
    - name: SonarQube Scan for PullRequest
      if: ${{ github.event.number }}
      uses: risclog-solution/sonarqube-action@master
      with:
        host: https://sonarqube.risclog.fcio.net
        login: ${{ secrets.SONARQUBETOKEN }}
        projectType: ${{ inputs.project_type }}
        pullrequestKey: ${{ github.event.number }}
        pullrequestBranch: ${{ github.HEAD_REF }}
        pullrequestBase: ${{ github.BASE_REF }}
        pullrequestRepository: ${{ github.repository }}
